name: student_app
description: Upa! Estudiante - App móvil educativa gamificada
publish_to: 'none'
version: 0.1.0-m0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  dio: ^5.4.0
  go_router: ^14.0.0
  get_it: ^7.6.4
  flutter_secure_storage: ^9.0.0
  socket_io_client: ^2.0.3+1
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
